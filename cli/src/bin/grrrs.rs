fn main() {
    let pattern = std::env::args().nth(1).expect("no pattern given");
    let path = std::env::args().nth(2).expect("no path given");

    // following https://rust-cli.github.io/book/tutorial/cli-args.html
}
